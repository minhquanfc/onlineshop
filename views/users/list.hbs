<div class="datalist">
    <div class="col-12" id="headerlist">
        <div class="">
            <a href="/users/add" class="btn btn-primary">Thêm</a>

            <form class="d-flex" id="timkiem">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>

        </div>
    </div>
    <div class="tables">
        <h3>Danh sách</h3>
        <table class="table table-bordered">
            <thead>
            <tr>
                                <th>ID</th>
                <th>Tên</th>
                <th>Email</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            {{#each listUsers}}
                <tr>
                    <td scope="row">{{this._id}}</td>
                    <td scope="row">{{this.ten}}</td>
                    <td>{{this.email}}</td>
                    <td>
                        <a href="/users/edit/{{this._id}}" class="btn btn-primary" id="btnAction">Edit</a>
                        <a href="/users/delete/{{this._id}}" onclick="return confirm('Bạn có muốn xóa không?')" id="btnAction" class="btn btn-danger">Del</a>
                    </td>
                </tr>
            {{/each}}
            </tbody>
        </table>
    </div>
</div>